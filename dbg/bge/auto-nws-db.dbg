        1   block-level on error undo, throw.
        2   define output parameter odbList as character no-undo.
        3   for each  db where db.db-key ne "" no-lock:
        4         odbList =  odbList + "," + string(db.db-num).
        5   end.
        6   odbList = substring (odbList,3).
