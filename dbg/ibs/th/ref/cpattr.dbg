        1   using Progress.Lang.*.
        2   block-level on error undo, throw.
        3   class ibs.th.ref.cpattr:
        4     define property ReturnMsg as character no-undo
        5       get.
        6       private set.
        7     define variable hndl-proc-cpattr as handle.
        8     constructor public cpattr ():
        9       run ref\cp-attr.p persistent (output hndl-proc-cpattr) no-error.
       10     end constructor.
       11     method public void CpAttrCode (
       12       input     p-code        as character,
       13       output    p-type        as character,
       14       output    p-format      as character,
       15       output    p-label       as character,
       16       output    p-range       as integer,
       17       output    p-user-can-edit as logical,
       18       output    p-output-display  as logical,
       19       output    p-other       as character):
       20       run cp-attr-code in hndl-proc-cpattr (
       21       input     p-code,
       22       output    p-type,
       23       output    p-format,
       24       output    p-label,
       25       output    p-range,
       26       output    p-user-can-edit,
       27       output    p-output-display,
       28       output    p-other) no-error.
       29       if error-status:error
       30         then
       31       do:
       32       end.
       33     end.
       34     method public void CpAttrTooltip (
       35       input   p-code      as character,
       36       output  p-tooltip   as character,
       37       output  p-label     as character
       38       ):
       39       run cp-attr-tooltip in hndl-proc-cpattr (
       40         input   p-code,
       41         output  p-tooltip,
       42         output  p-label) no-error.
       43       if error-status:error
       44         then
       45       do:
       46       end.
       47     end.
       48       method public void CpAttrValue (
       49       input    p-cdpay-code     as integer,
       50       input    p-curr-code      as integer,
       51       input    p-host-code      as integer,
       52       input    p-obj-type       as character,
       53       input    p-obj-code       as integer,
       54       input    p-code           as character,
       55       output   p-value          as character,
       56       output   p-type           as character):
       57       run cp-attr-value in hndl-proc-cpattr (
       58       input     p-cdpay-code,
       59       input     p-curr-code,
       60       input     p-host-code,
       61       input     p-obj-type,
       62       input     p-obj-code,
       63       input     p-code,
       64       output    p-value,
       65       output    p-type) no-error.
       66       if error-status:error
       67         then
       68       do:
       69       end.
       70     end.
       71     method public void CpAttrWrite (
       72       input    p-cdpay-code     as integer,
       73       input    p-curr-code      as integer,
       74       input    p-host-code      as integer,
       75       input    p-obj-type       as character,
       76       input    p-obj-code       as integer,
       77       input    p-code           as character,
       78       input    p-value          as character):
       79       run cp-attr-write in hndl-proc-cpattr (
       80       input     p-cdpay-code,
       81       input     p-curr-code,
       82       input     p-host-code,
       83       input     p-obj-type,
       84       input     p-obj-code,
       85       input     p-code,
       86       input     p-value) no-error.
       87       if error-status:error
       88         then
       89       do:
       90       end.
       91     end.
       92     method public void CpAttrExist (
       93       input    p-cdpay-code     as integer,
       94       input    p-curr-code      as integer,
       95       input    p-host-code      as integer,
       96       input    p-obj-type       as character,
       97       input    p-obj-code       as integer,
       98       input    p-code           as character,
       99       output   p-exist          as logical):
      100       run cp-attr-exist in hndl-proc-cpattr (
      101       input     p-cdpay-code,
      102       input     p-curr-code,
      103       input     p-host-code,
      104       input     p-obj-type,
      105       input     p-obj-code,
      106       input     p-code,
      107       output    p-exist) no-error.
      108       if error-status:error
      109         then
      110       do:
      111       end.
      112     end.
      113     method public void CpAttrDelete (
      114       input    p-cdpay-code     as integer,
      115       input    p-curr-code      as integer,
      116       input    p-host-code      as integer,
      117       input    p-obj-type       as character,
      118       input    p-obj-code       as integer,
      119       input    p-code           as character,
      120       output   p-deleted        as logical):
      121       run cp-attr-delete in hndl-proc-cpattr (
      122       input     p-cdpay-code,
      123       input     p-curr-code,
      124       input     p-host-code,
      125       input     p-obj-type,
      126       input     p-obj-code,
      127       input     p-code,
      128       output    p-deleted) no-error.
      129       if error-status:error
      130         then
      131       do:
      132       end.
      133     end.
      134     method public void CpAttrNews (
      135       input   p-code      as character,
      136       output  p-news      as logical
      137       ):
      138       run cp-attr-news in hndl-proc-cpattr (
      139         input   p-code,
      140         output  p-news) no-error.
      141       if error-status:error
      142         then
      143       do:
      144       end.
      145     end.
      146     method public void CpAttrHist (
      147       input   p-code      as character,
      148       output  p-hist      as logical
      149       ):
      150       run cp-attr-hist in hndl-proc-cpattr (
      151         input   p-code,
      152         output  p-hist) no-error.
      153       if error-status:error
      154         then
      155       do:
      156       end.
      157     end.
      158     destructor public cpattr ():
      159       delete object hndl-proc-cpattr no-error.
      160       if error-status:error
      161         then
      162       do:
      163       end.
      164     end destructor.
      165   end class.
