        1   using Progress.Lang.*.
        2   block-level on error undo, throw.
        3   class ibs.th.str.placelib:
        4     define property ReturnMsg as character no-undo
        5       get.
        6       private set.
        7     define variable hndl-proc-placelib as handle.
        8     constructor public placelib ():
        9       run str\placelib.p persistent (output hndl-proc-placelib) no-error.
       10     end constructor.
       11     method public void placelib_get-attr (
       12       input p-code as character,
       13       input p-obj-code as integer,
       14       input p-obj-type as character,
       15       input p-pl-code  as integer,
       16       output v-value   as character,
       17       output p-ok      as logical):
       18       run placelib_get-attr in hndl-proc-placelib (
       19         input p-code,
       20         input p-obj-code,
       21         input p-obj-type,
       22         input p-pl-code,
       23         output v-value,
       24         output p-ok) no-error.
       25       if error-status:error
       26         then
       27       do:
       28       end.
       29     end.
       30     method public void placelib_write-attr (
       31       input p-code as character,
       32       input p-obj-code as integer,
       33       input p-obj-type as character,
       34       input p-pl-code  as integer,
       35       input v-value   as character,
       36       output p-ok      as logical):
       37       run placelib_write-attr in hndl-proc-placelib (
       38         input p-code,
       39         input p-obj-code,
       40         input p-obj-type,
       41         input p-pl-code,
       42         input v-value,
       43         output p-ok) no-error.
       44       if error-status:error
       45         then
       46       do:
       47       end.
       48     end.
       49     destructor public placelib ():
       50       delete object hndl-proc-placelib no-error.
       51       if error-status:error
       52         then
       53       do:
       54       end.
       55     end destructor.
       56   end class.
